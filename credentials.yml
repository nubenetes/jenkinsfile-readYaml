# List of OCP Tokens/credentials
DEV:
  #OCP_CLUSTER_A:
    #ocp_token_local: 'TOKEN-A-local'
    #ocp_token_global: 'TOKEN-A-global'
  OCP_CLUSTER_C: # OCP 4
    ocp_token_local: 'TOKEN-C-local'
    ocp_token_global: 'TOKEN-C-global'
UAT:
  OCP_CLUSTER_A:
    ocp_token_local: 'TOKEN-A-local'
    ocp_token_global: 'TOKEN-A-global'
PRE:
  OCP_CLUTER_A: # PaaS 2.0 (OCP 4)
    ocp_token_local: 'TOKEN-A-local'
    ocp_token_global: 'TOKEN-A-global'
PRO:
  OCP_CLUSTER_A:
    ocp_token_local: 'TOKEN-A-local'
    ocp_token_global: 'TOKEN-A-global'
  OCP_CLUSTER_B:
    ocp_token_local: 'TOKEN-B-local'
    ocp_token_global: 'TOKEN-B-global'
